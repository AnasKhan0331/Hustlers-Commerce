'use client';
import { Typography, useTheme } from '@mui/material';
import React from 'react'

const Home = () => {
    const theme = useTheme();

    return (
        <div>
            Home
        </div>)
}

export default Home